"use client"

export default function Chatbot() {
  // Disabled: WiseCase uses the Legal RAG Assistant launcher from app/layout.tsx.
  // This inert component prevents any stale Chatbot import from rendering the old widget.
  return null
}
